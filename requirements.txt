# Minimum versions only — pip will resolve the newest release that is
# compatible with whichever Python interpreter runs this (3.9 through 3.13+).
# No upper caps, so this keeps working as new Python/library versions ship.
streamlit>=1.28
pandas>=1.5
plotly>=5.15
scikit-learn>=1.1
openpyxl>=3.0
numpy>=1.23
wordcloud>=1.9
vaderSentiment>=3.3
